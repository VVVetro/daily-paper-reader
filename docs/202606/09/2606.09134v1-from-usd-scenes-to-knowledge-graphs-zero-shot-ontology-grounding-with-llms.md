---
title: "From USD Scenes to Knowledge Graphs: Zero-Shot Ontology Grounding with LLMs"
title_zh: 从USD场景到知识图谱：基于大语言模型的零样本文体接地
authors: "Jiangtao Shuai, Zongxiong Chen, Manfred Hauswirth, Sonja Schimmler"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09134v1"
tags: ["query:smart-city"]
score: 8.0
evidence: 利用LLM从3D场景零样本生成本体标注并构建知识图谱
tldr: "现有知识图谱构建依赖手动词典将场景对象对齐到本体类，缺乏通用性。本文提出利用大型语言模型（LLM）实现零样本对齐，无需训练。在包含125个对象的厨房场景中，LLM在描述性名称下达到90-96%的精确匹配准确率，缩写名称下为49-89%，显著优于词典和嵌入基线。通过上下文增强提示，即使完全模糊名称也能恢复至48%准确率。消融实验表明，LLM主要依赖场景图中的语义线索（兄弟名称和父路径），匿名化后准确率骤降至0-6%，而仅用几何信息仅得到4-17%。该工作为自动化、可泛化的场景本体对齐提供了新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09134v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 759, \"height\": 769, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09134v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 641, \"height\": 708, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09134v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 588, \"height\": 335, \"label\": \"Table\"}]"
motivation: 场景对象到本体类的对齐依赖手动词典，无法泛化到新资产，成为知识图谱构建的瓶颈。
method: 利用LLM进行零样本对齐，通过描述性名称或缩写名称的提示，并结合上下文增强（如场景图语义线索），无需训练。
result: "在125个对象的厨房场景中，描述性名称准确率90-96%，缩写名称49-89%，匿名名称借助上下文恢复至48%，远超基线。"
conclusion: LLM能有效自动化本体对齐，主要依赖语义线索；几何信息贡献有限，匿名化后性能大幅下降。
---

## 摘要
从3D仿真场景构建知识图谱对于机器人任务推理至关重要，但关键瓶颈——将场景对象接地到正式本体类——仍然依赖于手动策划的词典，这些词典脆弱且无法跨资产泛化。我们研究了大语言模型（LLMs）是否能够自动化Universal Scene Description (USD)场景的这一接地步骤，作为一种零样本、无需训练的替代方案。在带有SOMA-HOME本体的厨房场景（125个对象）中，LLMs在描述性名称上达到90-96%的精确匹配准确率，在缩写名称上达到49-89%，显著优于词典和嵌入基线。在完全模糊的名称下，上下文增强提示恢复率高达48%。特征消融表明，LLMs主要利用场景图中的语义线索（兄弟名称和父路径）；匿名化这些线索将准确率降至0-6%，而仅凭几何信息只有4-17%。

## Abstract
Constructing knowledge graphs from 3D simulation scenes is essential for robot task reasoning, but the key bottleneck, grounding scene objects to formal ontology classes, still relies on manually curated dictionaries that are brittle and do not generalize across assets. We investigate whether large language models (LLMs) can automate this grounding step for Universal Scene Description (USD) scenes as a zero-shot, training-free alternative. On a kitchen scene (125 objects) with SOMA-HOME Ontology, LLMs achieve 90-96% exact-match accuracy with descriptive names and 49-89% with abbreviated names, substantially outperforming dictionary and embedding baselines. Under fully opaque names, context-augmented prompting recovers up to 48%. Feature ablation reveals that LLMs primarily exploit semantic cues in the scene graph (sibling names and parent paths); anonymizing these cues reduces accuracy to 0-6%, while geometry alone yields only 4-17%.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：从3D仿真场景（特别是Universal Scene Description, USD）构建知识图谱时，关键步骤是将场景中的对象（prim）映射到正式本体类（即“接地”，Grounding），但传统方法依赖手工策划的词典。这种方式脆弱、无法跨资产泛化，成为知识图谱构建的瓶颈。
- **研究动机**：机器人任务推理需要语义化的环境知识，而数字孪生中语义潜力未被充分挖掘。现有方法（如Multiverse框架）需要专家手动维护映射字典，无法随场景规模扩展。
- **整体含义**：探索是否能够利用大语言模型（LLM）的预训练世界知识，实现零样本、无需训练的自动本体接地，以替代手工词典。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将USD场景中每个prim的结构化属性（名称、几何、位置、层次关系）与线性化的本体TBox（类列表+自然语言描述）组合成自然语言提示，输入LLM，由LLM基于其预训练知识直接预测最匹配的本体类。
- **关键技术细节**：
  - **Context Extraction from USD**：对每个prim提取三组特征：
    1. 几何：轴对齐包围盒（宽、高、深）和质量（若可用）；
    2. 位置：世界坐标平移；
    3. 层次：父路径和兄弟名称。
  - **TBox Linearization**：将本体所有相关类（包括叶节点和中间节点）平铺为列表，每个类附带父类和自然语言描述（来自rdfs:comment）。
  - **Prompting Strategies**：三种递增丰富性的提示策略：
    - （A）仅名称 + 线性化TBox；
    - （B）上下文增强：加入几何、位置、层次信息；
    - （C）思维链（CoT）：要求模型逐步推理后再输出JSON映射。
  - **零样本映射**：LLM根据提示中的信息，从类列表中选出最合适的类；若没有叶类匹配，则选择最具体的适用超类。
- **无训练**：不进行任何微调或参数更新。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：Pixar Kitchen Set USD资产（来自Pixar），从中提取125个对象级prim（从约2700个场景prim中筛选），涵盖家具、电器、容器、餐具、工具、装饰等类别。目标本体：SOMA-HOME（94个厨房相关类，层级深度≤5）。
- **命名制度**：为模拟不同命名质量，定义三种命名制度：
  - **语义（Semantic）**：保留原始标识（如`CupCBlue1`）；
  - **缩写（Abbreviated）**：去元音并截断（如`cpcbl1`）；
  - **模糊（Opaque）**：替换为顺序标识（如`obj042`）。
- **基准方法**：
  - **Multiverse**：基于精确字符串查找的词典映射；
  - **S-BERT**：使用Sentence-BERT嵌入计算prim名称与类名的余弦相似度；
  - **LV-NN**：基于分割框体积的最近邻匹配（仅几何信息）。
- **对比LLM**：
  - Gemini 2.5 Flash、Gemini 3 Flash、Qwen 3.5-27B（包括思考模式Q-T和指令模式Q-NT）。
  - 所有模型温度=0，确定性解码。

### 4. 资源与算力

- **文中未明确说明**使用了何种GPU型号、数量或训练时长（因为方法是零样本、无训练）。仅提到所有实验在温度=0下进行单一确定性运行。因此，无法报告算力细节。

### 5. 实验数量与充分性

- **实验数量**：涵盖三大命名制度（语义、缩写、模糊）× 三种提示策略（A、B、C）× 四个模型变体（Gemini 2.5F、3F、Q-NT、Q-T），共约36组实验。此外，额外进行了**特征消融实验**（表II），在模糊命名下仅提供单一特征子集（几何、层次、位置）或匿名化版本，并测试了不同条件。
- **充分性**：实验设计较为全面：包含了非LLM基准、多模型对比、三种命名质量、三种提示复杂度，以及详细的消融分析。误差分析也覆盖了典型失败模式。总体客观公平，但仅局限于单一厨房场景（125对象），泛化性未验证。

### 6. 论文的主要结论与发现

- **LLM可有效零样本接地**：在语义命名下，所有LLM达到90-96%精确匹配准确率；缩写命名下49-89%，远超词典基线（9%）和嵌入基线（67%）。
- **上下文增强可部分弥补名字缺失**：在完全模糊命名下，仅用名称准确率仅6%，加入上下文（几何、层次）后，最佳模型（Gemini 3F）提升至48%。
- **思维链（CoT）效果不一致**：仅在Qwen指令模式下（无内置推理）有提升（+9%），其他模型无稳定收益，甚至略有下降。
- **场景图语义线索是主要驱动力**：特征消融表明层次信息（父路径和兄弟名称）贡献最大（21-54%），而几何只有4-17%，位置仅4-9%。匿名化层次信息后准确率骤降至0-6%，证明LLM主要依赖语义线索而非几何推理。
- **失败模式**：超类崩溃——几何相似的物体（如杯子、碗、花瓶）被映射到浅层超类；模糊命名下，尺寸独特的物体（如冰箱、扫帚）正确识别率高。

### 7. 优点：方法或实验设计上的亮点

- **零样本无训练**：完全避免手工词典和标注成本，可直接用于新场景。
- **系统性消融**：通过三种命名制度、三种提示策略、特征子集消融，清晰揭示了不同信息源（名称、语义场景图、几何）的相对贡献。
- **多模型对比**：涵盖不同系列（Gemini、Qwen）和模式（思考/指令），增强结论可靠性。
- **引入层级F1指标**：比精确匹配更细粒度，能部分奖励接近正确类的预测。
- **公开代码**：提供复制包，促进可重复性。

### 8. 不足与局限

- **实验覆盖有限**：仅测试单一厨房场景（125对象），领域多样性不足（未涉及仓库、办公室等），泛化性未知。
- **无方差估计**：所有模型以温度=0运行一次，未报告随机种子或多次运行的标准差，统计可靠性待提高。
- **模糊命名制度不彻底**：仅目标prim名称匿名化，但场景图中的兄弟名称和父路径仍保留语义；完全匿名化场景可能更准确地隔离几何推理贡献。
- **TBox线性化方式**：未保留本体层次结构，可能影响模型对超类的选择，但增加提示长度。
- **思考模式退化**：Qwen 3.5-27B在模糊命名下陷入重复循环和无语义输出，显示某些LLM在极端信号缺失时可能失败。
- **未考虑多模态信息**：未利用渲染图像或视觉特征，仅依赖文本属性。

（完）
