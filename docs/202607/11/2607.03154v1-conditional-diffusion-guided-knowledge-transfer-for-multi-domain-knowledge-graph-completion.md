---
title: Conditional Diffusion Guided Knowledge Transfer for Multi-Domain Knowledge Graph Completion
title_zh: 条件扩散引导的知识迁移用于多领域知识图谱补全
authors: "Jiawei Sheng, Taoyu Su, Xixun Lin, Xiaodong Li, Tingwen Liu"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03154v1"
tags: ["query:smart-city"]
score: 8.0
evidence: 条件扩散知识图补全，可直接用于智慧城市知识图推理
tldr: "现有多领域知识图谱补全方法通过跨KG等价实体一致性约束转移知识，但易抑制实体领域特定信息。本文开创生成式范式，提出DMKGC框架：将每个KG视为实体完整信息的部分视图，以条件扩散模型生成域通用嵌入，通过先验嵌入确保无偏性并保留领域信息。在3个基准的14个KG上，尾部实体预测平均MRR提升4.3%，并在低资源数据下持续获益，验证了生成式迁移的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03154v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 860, \"height\": 289, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03154v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 311, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03154v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1794, \"height\": 531, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03154v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 859, \"height\": 314, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03154v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 857, \"height\": 317, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03154v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 871, \"height\": 548, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03154v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1848, \"height\": 574, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03154v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1851, \"height\": 573, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03154v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 898, \"height\": 431, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03154v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 879, \"height\": 484, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03154v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 879, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03154v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1850, \"height\": 380, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03154v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 898, \"height\": 629, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03154v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 876, \"height\": 465, \"label\": \"Table\"}]"
motivation: 现有方法通过一致性约束转移知识，但抑制了实体领域特定信息，阻碍性能提升，尤其在低资源场景。
method: 提出DMKGC，将支持KG的等价实体融合作为条件，利用扩散模型生成域通用实体嵌入，并借助先验嵌入和跨KG预测保留领域信息。
result: "在3个基准的14个KG上，尾部实体预测平均MRR提升4.3%，低资源设置下持续获益。"
conclusion: 生成式迁移范式可有效缓解一致性约束带来的信息抑制问题，提升多领域知识图谱补全性能。
---

## 摘要
多领域知识图谱补全（MKGC）旨在通过从其他支持知识图谱迁移知识来提升目标知识图谱中缺失三元组的预测能力。现有方法通常对跨知识图谱的等价实体施加一致性约束以迁移知识，但这可能会抑制实体的领域特定上下文信息。这种设计也可能损害来自所有知识图谱领域的实体表示信息，从而阻碍性能提升，尤其是在低资源数据场景下。为解决这一问题，我们开创性地提出了基于生成的MKGC范式，并设计了DMKGC——一种条件扩散引导的知识迁移框架。我们的关键洞察是将每个知识图谱视为实体全局信息的局部视图，并通过基于支持知识图谱的条件扩散模型生成信息丰富的领域通用实体嵌入。具体而言，我们首先将领域无关的实体嵌入初始化为先验实体嵌入，然后在各知识图谱内对其进行编码。之后，我们将来自支持知识图谱的等价实体融合作为条件扩散生成引导。我们利用先验实体嵌入作为代理生成目标，确保该条件生成不会偏向任何条件知识图谱。同时，我们训练生成的嵌入能够跨知识图谱进行预测，从而保留领域特定信息。在3个基准测试的14个知识图谱上的大量实验表明，在尾部实体预测中，平均MRR比最先进方法提升4.3%，并且在低资源数据设置下持续保持增益。

## Abstract
Multi-domain knowledge graph completion (MKGC) aims to improve missing triple prediction in a target KG by transferring knowledge from other support KGs. Existing methods typically enforce consistency constraints on equivalent entities across KGs to transfer knowledge, which risks suppressing domain-specific contextual information of entities. This design can also compromise entity representation information from all KG domains, impeding performance improvements, especially in low-resource data scenarios. To address this, we pioneer a generation-based paradigm for MKGC and propose DMKGC, a conditional diffusion-guided knowledge transfer framework. Our key insight is to treat each KG as a partial view of the entity entire information, and generate informative domain-general entity embeddings through diffusion models conditioned on support KGs. Particularly, we first initialize domain-agnostic entity embeddings as prior entity embeddings, and then encode them within individual KGs. Afterward, we fuse equivalent entities from support KGs as the conditional diffusion generation guidance. We leverage the prior entity embeddings as the proxy generation objective, which ensures this conditional generation to be unbiased towards any conditioned KGs. Simultaneously, we also train the generated embeddings to be predictive across KGs, thus preserving domain-specific information. Extensive experiments on 14 KGs in 3 benchmarks demonstrate a 4.3\% average MRR improvement in tail entity prediction over state-of-the-art methods, with sustained gains in low-resource data settings.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）
- 任务：多领域知识图谱补全（MKGC），即利用多个相关支持知识图谱（Support KGs）中的知识，提升目标知识图谱（Target KG）中缺失三元组（尤其是尾部实体）的预测能力。
- 现有方法的局限：主流方法采用一致性约束（consistency-based）范式，通过约束等价实体（equivalent entities）的嵌入一致性来迁移知识。但这种强制对齐会抑制实体的领域特定上下文信息，导致嵌入过于平滑，尤其在低资源数据场景下严重阻碍性能提升。
- 核心洞察：将每个知识图谱视为实体完整信息的局部观测视图，通过生成式范式（generation-based）学习领域通用（domain-general）实体嵌入，既能保留各领域的独有信息，又能实现无偏知识迁移。
- 整体思路：首次在MKGC中引入条件扩散模型（conditional diffusion models），以支持KG的等价实体信息为条件，生成信息丰富的领域通用嵌入。

## 2. 论文提出的方法论
- **核心思想**：设计DMKGC框架，包含三部分：多领域实体编码器、条件扩散迁移模块、联合训练与推理。
- **关键技术细节**：
  - **多领域实体编码器**：
    - 先验实体嵌入（Prior Entity Embedding）：共享的随机初始化嵌入 `\tilde{e}`，不依赖任何具体KG，作为无偏生成目标。
    - 域实体嵌入（Domain Entity Embedding）：对每个KG，使用具有注意力机制的图编码器（公式5）聚合邻居（头实体、关系、尾实体），输出域感知嵌入 `z_t` 和 `z_s`。
  - **条件扩散迁移**：
    - 前向过程：以先验嵌入 `\tilde{e}` 为初始 `x_0`，按扩散步长加噪得到 `x_k`（公式6）。
    - 逆向过程：以加噪嵌入 `x_k` 为起点，利用条件去噪器（conditional denoiser）逐步去噪，还原至 `x_0`。
    - 条件去噪器（公式8）：MLP融合 `x_k`、条件嵌入 `c` 和步长嵌入 `k`。条件 `c` 分为三种情况：无条件时使用零嵌入 `\phi`；来自单个KG时直接用 `z_i`；来自多个支持KG时，通过注意力机制（公式9）动态融合各等价实体嵌入 `z_s`，得到 `\bar{z}_s`。
    - 训练中采用直接预测初始嵌入 `\hat{x}_\theta` 的简化形式（公式13），而非预测噪声。
  - **训练目标**（公式19）：
    - `L_gen`：域通用嵌入生成损失——以支持KG融合条件 `\bar{z}_s` 指导生成，使输出逼近先验嵌入 `\tilde{e}`，确保无偏性（公式14）。
    - `L_task`：目标域任务预测损失——将生成的嵌入 `\hat{x}_\theta`、先验嵌入 `\tilde{e}`、目标域嵌入 `z_t` 相加得到最终混合嵌入 `\bar{z}_t`，再用基于负采样的边际损失（公式16-17）优化KGC预测。
    - `L_reg`：单域条件正则化（公式18）——分别以每个KG的独立等效嵌入 `z_i` 为条件，生成结果同样逼近 `\tilde{e}`，增强生成稳定性与一致性。
    - 联合训练时使用分类器无关引导（CFG）策略，随机比例 `p_u` 放弃条件，提升无条件生成能力。
- **推理流程**（算法2）：从 `x_T` 开始逆向去噪，并用CFG插值调整条件强度，最终得到 `\hat{x}_0`，再与 `\tilde{e}` 和 `z_t` 混合后用于打分。

## 3. 实验设计
- **数据集与场景**：
  - DBP-5L：5个多语言DBpedia子集（EL/EN/ES/FR/JA），共约8.7k~48.7k训练三元组。
  - E-PKG：6个语种的电商手机产品知识图谱（DE/EN/ES/FR/IT/JA），训练三元组10k~60k。
  - DWY：跨领域知识图谱（DBpedia, YAGO, Wiki），各约82k~91k三元组。
- **基准（Benchmark）**：共14个知识图谱，覆盖多语言、多领域场景，统一关系模式，且提供了预先对齐的等价实体对。
- **对比方法**：
  - 单域方法：TransE, DistMult, RotatE, KG-BERT。
  - 多域一致性方法：KEnS, CG-MuA, AlignKGC, SS-AGA, LSMGA, GLKGC。
  - 额外对比：将InfoNCE、DA-DIFF、MMD等通用知识迁移方法嵌入同一编码器骨架。

## 4. 资源与算力
- 论文未明确说明所使用的GPU数量、训练总时长等详细算力信息，仅提到实验在配备 **Tesla T4 GPU** 的服务器上运行，并指出优化器为Adam（学习率1e-3）。
- 在附录效率分析中，给出了每三元组的训练/推理时间（毫秒级），但未给出整体耗时；强调了DMKGC收敛更快（16轮最优，而对比方法需约47-49轮）。

## 5. 实验数量与充分性
- **实验组数量丰富**：
  - 主实验结果（表1-3）：在三大数据集共14个KG上报告Hits@1, Hits@10, MRR，并与多种基线对比。
  - 消融实验（表4）：移除扩散、替换编码器、移除条件或正则化、替换损失函数等共10种变体分析。
  - 与通用迁移方法对比（表5）。
  - 低资源场景实验（图4-5，表6）：限制等价实体比例（20%/50%/80%）、限制支持KG数量、测试未见实体（unseen entities）的泛化能力。
  - 超参数分析（图6）：对扩散步数T、条件强度s、损失权重ω1/ω2进行调参。
  - 效率对比（附录A.5.2）：训练时间与推理时间对比。
- **充分性与公平性**：
  - 基线结果尽可能从原文或重现得到，采用网格搜索调参（3次试验）。
  - 评价指标标准（MRR, Hits@K），遵循前人协议。
  - 消融实验中各组件贡献清晰，低资源场景设计合理。
  - 不足之处：部分基线结果来自原文未公开代码的情况由作者重现；无外推至更大规模KG的验证（如Wikidata级）。

## 6. 论文的主要结论与发现
- DMKGC在所有数据集上均显著优于现有一致性方法：DBP-5L上平均MRR提升5.7%（绝对），E-PKG提升2.5%，DWY提升4.7%，**整体平均提升4.3%**。
- 在低资源场景（少量等价实体、少量支持KG、未见实体）中，DMKGC保持甚至扩大优势，证明了生成式迁移的鲁棒性。
- 消融实验验证了条件扩散、先验嵌入引导、单向正则化、直接预测初始嵌入（而非噪声）等设计不可或缺。
- 相比通用迁移技术（InfoNCE、DA-DIFF、MMD），DMKGC通过条件扩散生成无偏且保留域信息的嵌入效果最佳。

## 7. 优点
- **创新性**：首次将MKGC任务从一致性范式推进到生成式范式，为知识迁移提供了全新视角。
- **方法论设计巧妙**：
  - 利用先验嵌入作为无偏生成目标，避免了条件过拟合到特定支持KG。
  - 通过任务预测损失保持生成嵌入的判别力，兼顾通用性与领域特异性。
  - 单域条件正则化进一步稳定生成过程。
  - 使用CFG控制条件强度，增强了生成灵活性。
- **实验全面且详实**：覆盖多语言、多领域、低资源等多种实际场景，消融及对比实验充分验证各组件贡献。
- **代码开源**（论文脚注中提供GitHub链接），促进可复现性。

## 8. 不足与局限
- **对齐依赖**：方法仍假设等价实体已预先对齐，对齐质量直接影响迁移效果；未讨论对齐错误时的鲁棒性。
- **推理效率**：扩散模型在推理时需多步逆向过程（T步），虽然步数可调（实验中T=16~32），相对基于简单嵌入融合的方法仍更耗时。
- **规模瓶颈**：实验中最具规模的数据集训练三元组约9万（DWY），未验证在千万级或亿级KG上的可扩展性。
- **关系模式假设**：所有KG使用统一关系模式，若关系异构严重（如不同领域间关系不重叠），方法可能无法直接应用。
- **与LLM方法的对比缺失**：未能与近期基于预训练语言模型或大语言模型的KGC方法进行比较（文中解释这些方法不属于当前多领域任务设定，但可作为未来工作）。
- **超参数敏感**：条件强度s、损失权重ω1/ω2需要针对不同数据集调参，增加应用成本。

（完）
