---
title: "OntoExtend: A Framework for Requirement-driven and Scalable Ontology Extension with LLMs"
title_zh: OntoExtend：一种基于需求且可扩展的LLM本体扩展框架
authors: "Anna Sofia Lippolis, Mohammad Javad Saeedizade, Stefan Schmid, Simon Blattner, Robin Keskisärkkä, Aldo Gangemi, Eva Blomqvist, Andrea Giovanni Nuzzolese"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17963v1"
tags: ["query:smart-city"]
score: 8.0
evidence: 基于LLM的本体扩展框架，可直接用于智慧城市数据的知识图谱构建
tldr: 本体扩展是资源密集且易错的任务，现有LLM方法缺乏需求驱动和系统评估。本文提出OntoExtend框架，利用检索增强生成（RAG）结合输入本体和以能力问题表示的需求进行扩展。在39个需求上评估，生成片段结构问题少，满足所有功能测试，需少量到中等修改。该框架作为需求驱动本体扩展的起草助手有效，且对需求特异性和建模配置敏感。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17963v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1276, \"height\": 455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17963v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1274, \"height\": 173, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17963v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1249, \"height\": 336, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17963v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1169, \"height\": 1103, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17963v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 895, \"height\": 320, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17963v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1101, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17963v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1282, \"height\": 241, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17963v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 969, \"height\": 344, \"label\": \"Table\"}]"
motivation: 现有本体扩展缺乏需求驱动和可重用核心模型，且缺乏对LLM输出的系统评估。
method: 提出OntoExtend，使用检索增强生成（RAG）结合输入本体和能力问题需求生成扩展片段。
result: 在39个能力问题上评估，生成片段结构问题少，满足所有功能测试，被专家评为需少量到中等修改。
conclusion: OntoExtend作为需求驱动本体扩展的起草助手真实有效，且对需求特异性和建模配置敏感。
---

## 摘要
本体扩展是指根据新出现需求丰富现有本体，使其更完整的过程。这是一个资源密集且易出错的任务。大型语言模型（LLM）在从头生成本体方面表现出色，但现有方法很少将本体扩展明确地与需求或可重用核心模型挂钩，并且对LLM输出的系统评估有限。本文介绍了OntoExtend，一种基于需求驱动的LLM本体扩展框架。它利用检索增强生成（RAG）技术，在相关输入本体和以能力问题形式呈现的需求上进行检索，从而提出基于基础的扩展。我们针对两个用例中的39个能力问题评估了OntoExtend：一个公共欧盟项目本体Onto-DESIDE和一个来自博世的工业本体。生成的本体片段几乎没有结构问题，通过了所有功能评估测试，并被本体工程师评为在集成前只需进行轻微到中等程度的修改。这些结果表明，OntoExtend在实际场景中作为需求驱动本体扩展的起草助手非常有用，同时保持了对能力问题特异性和建模风格的敏感性。

## Abstract
Ontology extension refers to the process of enriching an existing ontology in response to emerging requirements, making it more complete. This task is a resource-intensive and error-prone process. Large Language Models (LLMs) have shown promising performance on generating ontologies from scratch, but current approaches rarely tie ontology extension explicitly to requirements or reusable core models, and offer limited, systematic evaluation of LLM outputs. This paper introduces OntoExtend, a requirements-driven framework for ontology extension with LLMs. It uses retrieval-augmented generation (RAG) over relevant input ontologies and requirements in the form of competency questions to propose grounded extensions. We evaluate OntoExtend on 39 CQs from two use cases: a public EU-project ontology, Onto-DESIDE, and an industrial ontology from Bosch. The generated fragments show few structural issues, satisfy all functional evaluation tests, and are rated by ontology engineers as requiring minor to moderate revision before integration. These results suggest that OntoExtend is useful as a drafting assistant for requirement-driven ontology extension in real world scenarios, while remaining sensitive to CQ specificity and modelling profile.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：本体扩展（Ontology Extension）是指根据新出现的需求对现有本体进行丰富，使其更完整。这一过程资源密集且易出错。
- **现有局限**：现有LLM方法虽然能在生成本体方面表现良好，但很少将本体扩展明确地与需求（如能力问题CQ）或可重用核心模型挂钩，且对LLM输出的系统评估有限。
- **目标**：开发一个需求驱动的、可规模化的大语言模型（LLM）本体扩展框架，使其能够基于现有本体和新的能力问题生成结构正确、功能完备的扩展片段，并降低人工成本。

## 2. 方法论：核心思想、关键技术细节与流程
- **核心思想**：采用检索增强生成（RAG）策略，先从一个或多个输入本体中检索与给定CQ最相关的实体（类、属性、公理），然后将这些实体作为上下文提供给LLM，驱动LLM生成缺失的本体片段，最后将这些片段集成回原本体。
- **框架关键模块**：
  1. **Ontology Retriever（本体检索器）**：
     - 预处理阶段解析每个本体文件，提取所有OWL实体（类、对象属性、数据属性、注释等），并将其序列化为管道分隔的字符串（URI局部名称、标签、注释、类型、域/范围等）。
     - 使用句子嵌入模型对这些字符串进行嵌入，构建FAISS索引（内积相似度）。
     - 查询时利用用户提供的CQ嵌入进行最近邻搜索，返回top-k（默认20）个最相关实体。
  2. **Ontology Extender（本体扩展器）**：
     - 将检索到的实体按来源本体分组，渲染为Turtle代码片段，连同统一的前缀块和CQ一起组装成结构化提示。
     - 允许用户配置不同的提示模板（例如SHACL模板或OWL限制模板），以适应不同本体工程配置文件。
     - 包含两级验证器：Turtle语法检查 + 建模约定检查（如必须提供rdfs:domain/rdfs:range等）。验证失败则重试或标记。
  3. **Ontology Integrator（本体集成器）**：
     - 对生成的片段进行去重（移除重复公理、类、属性、前缀），然后将其与输入本体拼接。
     - 可选择将生成的片段重新索引到检索器中，以便后续CQ引用（本评估中禁用）。
- **算法流程**：输入(输入本体集合, CQ列表) → 逐个CQ处理：检索器检索相关实体 → 构建提示 → LLM生成Turtle片段 → 验证 → 集成 → 输出扩展后的本体（以及可选地重新索引）。

## 3. 实验设计：数据集、场景、基准与对比方法
- **数据集**：两个真实世界用例
  - **欧盟项目本体（Onto-DESIDE）**：关注循环经济数据互操作性，包含4个模块（共约75k、22k、25k、6k tokens），手动构建了20个CQ。
  - **工业本体（Bosch）**：制造工业配置，共22k tokens，19个CQ。
- **数据生成过程**：从每个本体中随机选择一组类，移除这些类及其相关属性（包括子类迭代），然后构造CQ，每个CQ询问被移除的类/属性原本要表示什么。
- **无显式外部基准**：主要与已有工作（如Lippolis et al. 2025，Saeedizade & Blomqvist 2024）进行非正式比较（例如对比OOPS!陷阱数、多余元素百分比）。
- **对比方法**：主要比较两种LLM：
  - o1-preview
  - GPT-5
- **配置调优**：先在一个小的开发集（5个CQ，不包含在测试集）上对嵌入模型和提示进行调优：
  - 对比了 text-embedding-3-small、text-embedding-3-large、text-embedding-ada-002 以及分隔符（管道 vs 换行）、是否包含注释等12种配置，最终选择 text-embedding-ada-002 + 管道分隔 + 包含注释（加权指标Mw=0.63）。
  - 对提示也进行了迭代优化。

## 4. 资源与算力
- **论文未明确说明**使用的具体GPU型号、数量或训练时长。仅提到使用OpenAI的嵌入模型和LLM（API调用），未涉及训练。因此无具体算力细节。

## 5. 实验数量与充分性
- **实验数量**：
  - 检索配置调优：12种配置（3个嵌入模型 × 4种文本样式），人工评估5个CQ的P@3和P@20。
  - 主实验：2个用例 × 2个LLM × (20 + 19) = 共78次生成（每个CQ每个模型一次）。
  - 结构评估：对每个生成的扩展片段进行语法检查、OOPS!扫描和Pellet一致性检查。
  - 功能评估：对所有39个CQ进行CQ验证（SPARQL查询可回答性）和多余元素计数（由两位本体工程师独立交叉验证）。
  - 用户调查：6位本体工程师（每个用例3位）以Likert5点量表对每个片段进行正确性和完整性评分，并计算Fleiss加权一致系数。
- **充分性评价**：实验较为充分，覆盖了两个不同领域（学术/公共vs工业），涉及两种不同提示模板（OWL vs SHACL），多种评估维度（结构、功能、人工）。但缺乏消融实验（如是否使用RAG对比），也未在更多LLM模型（如开源模型）上验证。总体上设计合理，但可进一步扩展。

## 6. 主要结论与发现
- **结构质量**：生成的片段几乎无Turtle语法错误；OOPS!仅发现少量次要陷阱（如P02、P04、P08），未引入关键或重要问题，远优于以往从头生成的工作。
- **功能充分性**：所有39个CQ均被正确建模（CQ验证100%通过）；多余元素比例不到2%（远低于Lippolis et al. 2025报告的约30%）。
- **人工评价**：
  - 工业用例：正确性平均4.91–4.96，完整性平均4.54–4.56（五分制），评分高，需要微小修改即可集成。
  - 欧盟项目用例：正确性平均3.66–3.69，完整性平均2.94–3.11，评分中等，需要中等修改。
  - 差异原因：欧盟项目CQ更开放、更模糊，导致LLM需做出更多建模决策，而工业CQ更精确、范围更紧。
- **发现**：CQ的质量和清晰性显著影响结果；LLM性能可作为CQ质量的一种隐式代理。
- **实际影响**：OntoExtend作为起草助手有效，显著降低本体扩展的人工工作量和时间成本。

## 7. 优点
- **需求驱动**：首次将本体扩展明确与能力问题（CQ）和可重用核心模型结合。
- **可扩展性**：通过RAG避免将整个大本体输入LLM，节约时间和费用。
- **多维度评估**：结合结构（语法、OOPS!、一致性）、功能（CQ验证、多余元素）和人工评价（正确性、完整性及专家意见），体系完整。
- **实际场景验证**：在真实世界两个不同领域（公共研究项目和工业）的主动使用本体上测试，具有生态效度。
- **低多余元素率**（<2%）表明生成的扩展碎片基本是必要的，减少了后期清理工作。
- **模块化和可配置性**：允许用户切换提示模板以适应不同建模配置文件（OWL vs SHACL）。

## 8. 不足与局限
- **领域覆盖有限**：仅两个用例，结果泛化能力有待在更多领域验证。
- **LLM种类有限**：仅测试两个OpenAI闭源模型（o1-preview和GPT-5），未评估开源或不同规模模型，可能引入偏差。
- **数据泄漏风险**：欧盟项目本体公开，LLM预训练中可能已接触；作者虽提及但未提供严格预防措施。
- **CQ质量影响**：开放或模糊的CQ导致性能下降，框架对输入质量敏感，需要前期良好定义。
- **未做消融实验**：例如不进行RAG直接给完整本体的对比，或不同top-k值的影响未深入分析。
- **人工评估样本量小**：每个用例仅3位评价者，可能不够充分反映普遍观点；Fleiss权重一致系数在某些维度较高但仍需更多验证。
- **无开源模型对比**：未提供与开源LLM的可重复实验基线。

（完）
