---
title: Scalable Semantic Steering of Embedding Projections
title_zh: 嵌入投影的可扩展语义引导
authors: "Wei Liu, Eric Krokos, Kirsten Whitley, Rebecca Faust, Chris North"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03978v1"
tags: ["query:smart-city"]
score: 6.0
evidence: 基于LLM的可扩展语义引导嵌入投影方法，适用于城市数据表示
tldr: 针对嵌入投影的语义引导方法因逐项LLM推理导致成本随数据规模线性增长的问题，提出组级语义原型方法。通过单次LLM调用生成所有组的结构化画像，混合种子质心形成语义原型，利用软分配、弃权与对齐缩放实现无重训练传播。在5K文档LitCovid语料上，全局对齐性能与逐项方法相当，但LLM调用减少三个数量级。图像案例验证了方法对多模态嵌入的普适性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03978v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1657, \"height\": 386, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03978v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 189, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03978v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03978v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 877, \"height\": 275, \"label\": \"Table\"}]"
motivation: 现有语义引导依赖逐项LLM推理，成本随集合大小线性增长，难以扩展至大规模数据。
method: 单次LLM生成所有组的结构化画像，嵌入后与种子质心混合为语义原型，通过软分配、弃权与对齐缩放实现无重训练传播。
result: 在5K文档LitCovid语料上，全局对齐性能与逐项LLM引导相当，LLM调用减少三个数量级。
conclusion: 组级表示使语义引导在更大嵌入集合上更实用，并可扩展至多模态嵌入。
---

## 摘要
低维投影支持高维数据嵌入的交互式可视化分析，但其结构通常不符合分析人员定义的语义关系。最近的LLM增强语义引导方法通过从用户定义的种子示例组中外化分析人员意图来解决这一差距，但它们通过逐项LLM推理传播意图，导致LLM调用和成本随集合大小线性增长。我们提出了一种可扩展的语义引导方法，将语义计算从单个项目转移到用户定义的组。单个LLM调用为所有组生成结构化概要，这些概要被嵌入并与种子质心结合形成混合语义原型。然后，该方法无需重新训练即可传播意图，使用嵌入空间软分配、弃权和对齐缩放更新，然后重新投影。在5K文档的LitCovid语料库上，我们的方法实现了与逐项LLM引导相当的整体对齐，同时将LLM调用减少了三个数量级。一个图像案例研究表明，相同的基于原型的机制可以扩展到多模态嵌入。这些结果表明，组级表示可以使语义引导对于更大的嵌入集合更加实用。

## Abstract
Low-dimensional projections support interactive visual analysis of high-dimensional data embeddings, but their structure often does not align with analyst-defined semantic relationships. Recent LLM-augmented semantic steering methods address this gap by externalizing analyst intent from user-defined groups of seed examples, but they propagate intent through per-item LLM reasoning, causing LLM calls and cost to grow linearly with collection size. We propose a scalable semantic steering method that shifts semantic computation from individual items to user-defined groups. A single LLM call generates structured profiles for all groups, which are embedded and combined with seed centroids to form hybrid semantic prototypes. The method then propagates intent without retraining, using embedding-space soft assignment, abstention, and alignment-scaled updates before reprojection. On a 5K-document LitCovid corpus, our method achieves global alignment comparable to per-item LLM steering while reducing LLM calls by over three orders of magnitude. An image case study shows that the same prototype-based mechanism extends to multimodal embeddings. These results suggest that group-level representations can make semantic steering more practical for larger embedding collections.