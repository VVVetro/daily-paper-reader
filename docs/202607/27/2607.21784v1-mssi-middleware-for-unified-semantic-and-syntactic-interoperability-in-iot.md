---
title: "MSSI: Middleware for Unified Semantic and Syntactic Interoperability in IoT"
title_zh: MSSI：物联网中统一语义和语法互操作性的中间件
authors: "Sanku Kumar Roy, Sudip Misra, Narendra Singh Raghuwanshi"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21784v1"
tags: ["query:smart-city"]
score: 9.0
evidence: 物联网系统中语义和语法互操作性的中间件
tldr: "物联网中异构设备通信存在语义和句法差异，难以实现互操作。本文提出中间件MSSI，在发布-订阅框架中自动翻译消息的句法和语义：先通过句法转换解决数据格式差异，再使用多层感知机（MLP）模型将设备信息映射为用户所需语义，并设计特征提取算法。在多种数据格式与语义标注上评估，MLP分类准确率达95.78%。该中间件有效统一了异构设备的语义与句法，提升了物联网通信的可靠性与自主性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21784v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 839, \"height\": 484, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21784v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 941, \"height\": 288, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21784v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1456, \"height\": 178, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21784v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 285, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21784v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 880, \"height\": 1218, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21784v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 730, \"height\": 512, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21784v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 890, \"height\": 290, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21784v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 815, \"height\": 1552, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21784v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 931, \"height\": 948, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21784v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 668, \"height\": 412, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21784v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 585, \"height\": 144, \"label\": \"Table\"}]"
motivation: 物联网设备异构性导致语义和句法不兼容，影响通信的可靠性与自主控制，亟需统一的互操作中间件。
method: 提出句法翻译方法转换消息格式，并使用MLP模型实现语义转换，同时设计特征提取算法获取判别特征作为MLP输入。
result: "在公开数据格式和语义标注上，MLP分类准确率达95.78%，有效解决语义互操作问题。"
conclusion: 该中间件实现了物联网中异构设备间的统一语义与句法互操作，提升通信效率与普适性。
---

## 摘要
随着物联网（IoT）需求的增长，异构物联网设备与网络世界之间需要实现无缝可靠的通信，以确保对任何应用过程的自主控制。更具体地说，无缝通信需要在具有不同语义和数据格式（语法）的异构设备（执行器）之间实现互操作性，这使其更具挑战性。在本文中，我们提出了一种中间件解决方案，用于物联网网络发布-订阅框架中的统一语义和语法互操作性。所提出的框架自动将订阅者（用户）兼容的语法和语义从发布者（物联网设备）的接收消息中转换出来。首先，我们提出了一种新颖的消息语法转换方法，以解决用户和设备之间的语法差异，同时以用户请求的语法提供信息。然后，提出了一种基于多层感知器（MLP）的语义互操作性框架，将设备信息转换为用户请求的语义。此外，提出了一种新颖的算法来提取原始和判别性特征，这些特征将作为输入拟合到MLP模型中。为了展示所提出中间件的有效性，我们评估了不同的参数，同时考虑了各种公开使用的数据格式和属性的语义注释，以确保所提出中间件在实际场景中的通用性。使用MLP的整体分类准确率为95.78%，用于确定来自发布者传入消息的每个属性的标准含义，以解决物联网中的语义互操作性问题。

## Abstract
With the growing demand of Internet of Things (IoT), there is a need for seamless and reliable communication between heterogeneous IoT devices and the cyber-world to ensure autonomous control over any application process. More specifically, seamless communication requires interoperability between heterogeneous devices (actors) having different semantics and data formats (syntaxes), while making it more challenging. In this paper, we propose a middleware solution for unified semantic and syntactic interoperability in the publisher-subscriber framework of IoT network. The proposed framework automatically translates the subscribers (users) compatible syntax and semantics of the receiver message from the publishers (IoT devices). First, we propose a novel method of syntax translation of messages, to solve the syntactic disparities between users and devices, while providing the information in the user requested syntax. Thereafter, a multilayer perceptron (MLP)-based semantic interoperability framework is proposed to translate the device information to the user requested semantics. Additionally, a novel algorithm is proposed for extracting raw and discriminative features, which are to be fitted to the MLP model as inputs. To show the effectiveness of the proposed middleware, we evaluate different parameters, while considering various publicly used data formats and semantic annotations of attributes to ensure the versatility of the proposed middleware in the practical scenario. The overall classification accuracy using MLP is $95.78$\% for determining the standard meaning of each attribute of the incoming message from the publisher to address the semantic interoperability problem in IoT.

---

## 论文详细总结（自动生成）

# 论文总结：MSSI：物联网中统一语义和语法互操作性的中间件

## 1. 核心问题与整体含义（研究动机和背景）
- **背景**：物联网（IoT）中异构设备由不同厂商制造，使用不同的数据格式（语法，如JSON、XML、CSV）和不同的属性命名（语义）来表示相同或不同的概念。这导致设备之间以及设备与用户之间难以实现无缝通信。
- **核心问题**：在发布-订阅框架（PSF）中，发布者（设备）和订阅者（用户）可能使用完全不同的语法和语义，导致互操作性障碍。传统的基于本体或手动翻译的方法存在扩展性差（N²-N种翻译器）、依赖预先定义的公共本体、设备需要提前注册等问题。
- **论文目标**：提出一个中间件MSSI，能够在无需任何设备先验知识的情况下，自动将接收到的消息转换为订阅者（用户）兼容的语法和语义，实现统一的互操作性。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：分两步处理：先语法翻译，再语义翻译。
- **语法翻译**：
  - 将发布者发送的JSON或XML消息首先转换为统一的分层数据结构（中间表示），完成后再根据用户请求的格式（JSON或XML）反向转换。
  - 算法1（XML/JSON → 层次结构）：通过`isXML`函数识别消息格式（检查是否有开始/结束标签），然后验证并转换。算法2（层次结构 → XML/JSON）同理。
- **语义翻译**：
  - **特征提取**（核心贡献）：
    - **原始特征（Raw Features，26维）**：统计属性名中每个英文字母（a-z）的出现频率。
    - **判别性特征（Discriminative Features，5维）**：从属性值中提取：日期格式（日期数值）、时间格式（冒号的ASCII值）、单位（百分号的ASCII值）、纯数字值、以及去除特殊字符后整个字符串的ASCII码之和。总共31维特征向量。
  - **属性分类**：使用MLP（多层感知器）作为分类器（对比GMM和朴素贝叶斯）。MLP结构：输入层31个神经元，1个隐藏层25个神经元（经实验优化），输出层15个神经元（对应15个类别，如日期、时间、传感器名称、传感器值、单位、设备电压等）。训练算法：误差反向传播，Sigmoid激活函数，学习率0.1，动量0.2，迭代500轮。
  - **语义映射**：分类得到标准属性名后，根据用户定义的语义映射表，将标准名翻译为用户期望的属性名，并关联对应的值。

## 3. 实验设计
- **数据集**：
  - 公开数据集：Intel Lab Data、Murdoch University Weather Station。
  - 私有数据集：来自农业传感器网络（包含气温、湿度、太阳辐射等9种传感器）。
  - 将所有数据转换为JSON/XML格式。
  - 另外参考了论文[4][20][21]中定义的多种语义标注变种以增加多样性。
- **类别数量**：15个不同属性类别（如date, time, sensor name, sensor value, unit, device voltage等）。
- **数据量**：训练消息数2,633,597条，对应4,961,876个属性实例；测试消息754,183条，对应1,158,412个属性实例。
- **对比方法**：
  - GMM（高斯混合模型）：高斯个数从1到16变化。
  - 朴素贝叶斯（NB）。
  - MLP：隐藏层神经元数从20到35变化。
- **评价指标**：分类准确率、混淆矩阵、负对数似然（NLL）。

## 4. 资源与算力
- 论文中**未明确说明**所使用的GPU型号、数量、训练时长等计算资源。仅提及实验在MLP中使用的超参数（学习率、动量、迭代次数），未涉及硬件加速信息。

## 5. 实验数量与充分性评价
- **组实验数量**：
  - 对GMM：测试了不同高斯数（2~16）对准确率的影响。
  - 对MLP：测试了不同隐藏层神经元数（20~35）的影响。
  - 对三种分类器（GMM、NB、MLP）直接在测试集上比较准确率。
  - 给出了GMM和MLP的收敛曲线（负对数似然）以及三者的混淆矩阵。
  - 未进行显式的消融实验（如去掉判别特征或仅使用原始特征的效果对比）。
- **充分性与公平性**：
  - 数据集规模较大，覆盖多种传感器类型和多种语义变体，具有一定的通用性。
  - 对比方法选择合理（生成式GMM、朴素贝叶斯与判别式MLP），且均使用相同特征。
  - 缺点是没有与最新的基于Transformer或BERT的方法对比（可能当时未流行），也没有进行跨领域或跨数据集的泛化性测试。
  - 混淆矩阵显示少数类（如C6、C8）的TP率略低，但总体平衡。

## 6. 主要结论与发现
- **语法翻译**：提出的两步转换方法（JSON/XML ↔ 层次结构）能正确识别和转换不同数据格式。
- **语义翻译**：
  - MLP分类准确率最高，达**95.78%**，显著优于GMM（87.58%）和朴素贝叶斯（87.27%）。
  - 特征工程有效：26维原始字母频率+5维判别特征能够较好区分15个属性类别。
  - 混淆矩阵显示大部分类别TP率很高（≥95%），少量类（如C6设备电压、C8网络ID）略低但仍可接受。
- **实时性/应用**：文章未测量推理延迟，但指出该中间件适用于智慧工厂等需要实时数据翻译的场景。

## 7. 优点（方法/实验设计亮点）
- **新颖的特征提取**：同时利用属性名称中的字母频率（原始特征）和属性值中的模式（判别特征）来辅助分类，特别设计了日期、时间、单位等关键模式特征，提升了区分度。
- **无需先验知识**：与之前依赖于预定义本体或设备注册的工作不同，MSSI不需要任何设备语义或语法的先验知识，自动适应新设备。
- **统一处理语义与语法**：同时解决了两个层面的互操作问题，而非仅关注语义。
- **实验数据多样性**：使用多种公开和私有数据集，并人工加入不同的语义变体（如日期可以写为dat、date、SensDate等），增强了结果的可靠性。
- **收敛分析**：给出了GMM负对数似然随迭代收敛的曲线，证明模型参数稳定。

## 8. 不足与局限
- **语法格式有限**：只处理了JSON和XML两种格式，未涉及CSV、Protocol Buffers等常用格式。文中声称可扩展但未验证。
- **语义类别固定**：预设了15种属性类别，若出现全新类型的属性（如新传感器的特有字段），模型无法识别（会被错分给最相似的现有类）。
- **缺乏消融实验**：未单独验证26维原始特征和5维判别特征各自的贡献，也没有测试不同特征组合的效果。
- **缺乏实时性评估**：中间件的端到端翻译延迟、吞吐量等没有测量，实际部署时可能成为瓶颈。
- **部署依赖**：需要为每个新的用户语义映射提前配置映射表（语义mapper），用户体验可能受影响。
- **领域局限**：主要基于农业和气象传感器数据，对其他领域（如智能家居、工业自动化）的通用性未充分验证。
- **计算资源未报告**：无GPU/CPU训练时间，无法评估模型训练开销。

（完）
